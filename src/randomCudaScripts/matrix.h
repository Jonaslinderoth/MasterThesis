float* matMulSimple(float* a, float* b, int n);
float* matMulAdvanced(float* a, float* b, int n);
float matSum(float* v, int width);
bool matDiagEQ(float* v, float* w, int width);
void printMat(float* v, int width);
